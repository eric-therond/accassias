class stdlib
{
  function print(declr $string)
  {
    t_print
  }

  function system(declr $cmd)
  {
    t_system
  }

  function fputs(declr $file, declr $string)
  {
    t_fputs
  }

  function include(declr $file)
  {
    t_include
  }
}

declr $stdlib;
$stdlib = new stdlib;

